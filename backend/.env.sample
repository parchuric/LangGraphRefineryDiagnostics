# Server Configuration
PORT=3000
NODE_ENV=development

# PostgreSQL Database Configuration
# These are typical variables needed to connect to a PostgreSQL database.
# Apache AGE runs on top of PostgreSQL.
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=your_pg_database_name

# Apache AGE Specific Configuration
# This is the name of the graph within your PostgreSQL database.
DB_GRAPH_PATH=your_age_graph_name

# Optional: SSL Configuration for Database Connection
# Uncomment and configure these if your PostgreSQL server uses SSL.
# The presence of .crt and .pem files in backend/src suggests SSL might be used.
# DB_SSL_ENABLED=false
# DB_SSL_REJECT_UNAUTHORIZED=true
# DB_SSL_CA_PATH=src/DigiCertGlobalRootCA.crt
# DB_SSL_CERT_PATH=src/bundle.pem # Or your client certificate
# DB_SSL_KEY_PATH= # Path to your client key if required
